# Техническое задание: PDFsplitter

Два портативных `.exe` (всё внутри, без установки Python):

1. **PDFsplitter.exe** — консоль: пакетная нарезка по маске имени.
2. **PDFsplitterGUI.exe** — GUI: та же маска, плюс превью каждой страницы и правка имени до сохранения.

Код: `split_pdf.py` (движок + консоль), `split_gui.py` (окно). Сборка: `build.bat`. Очистка мусора: `clean.bat`.

## Маска имени

Маска = постоянный текст + переменные. Переменные две:

1. **Дата** `{DATE +ФОРМАТ}` — дата/время в виде формы strftime. Одна отметка на весь запуск (в GUI — на сессию). Пример: `{DATE +%d_%m_%y-%H%M%S}` → `30_08_26-143055`.
2. **Нумерация** `{НАЧАЛО:ШАГ}` — алгоритм: первый файл = НАЧАЛО, каждый следующий += ШАГ. Шаг может быть отрицательным; если опущен — `+1`. Пример: `{234:+1}` → 234, 235, 236.

Неизвестные `{…}` остаются как есть. Символы Windows `<>:"/\\|?*` после подстановки заменяются на `_`.

Пример: `"Акт о внедрении №{234:+1} {DATE +%d_%m_%y-%H%M%S}"`.

По умолчанию: `page_{1:+1}`.

## 3.4 Консоль (`split_pdf.py` → PDFsplitter.exe)

**S1.** Нарезка одного N-страничного PDF на N одностраничных PDF (движок — pikepdf).

**S2.** Имя каждого файла считается из маски. CLI: `-m` / `--mask`.

**S3.** Каталог вывода: `-d` / `--out-dir`, по умолчанию — рядом с исходным PDF.

Консоль не спрашивает имя: сохраняет сразу по маске.

## GUI (`split_gui.py` → PDFsplitterGUI.exe)

Сначала маска формирует имена **всех** страниц, потом при просмотре листа можно поправить **текущее** имя.

1. Указать PDF, папку и маску. Пример: `Акт о внедрении {216:+1} 27.06.2026`.
2. «Сформировать имена» — всем страницам: 216, 217, 218… и один и тот же постоянный текст (в том числе дата из маски).
3. Листать превью (масштаб: «+»/«−», Ctrl+колёсико; сдвиг: полосы прокрутки, колёсико, перетаскивание). Имя текущей страницы уже стоит из шаблона; его можно исправить, глядя на лист (например дата на 3-й странице другая → `Акт о внедрении 218 15.06.2026`).
4. «Сохранить и далее» — записать этот лист с (поправленным) именем и перейти к следующему. «Сохранить все» — записать все текущие имена разом.

В списке слева: пробел — как в шаблоне, `*` — имя поправили вручную, `✓` — уже сохранено.

`{DATE +формат}` по-прежнему можно использовать, если нужна сегодняшняя дата сразу во всех именах. Если даты разные на каждом акте — пишите дату обычным текстом в маске и правьте её по страницам.
