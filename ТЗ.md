# Техническое задание: PDFsplitter

Нарезка многостраничного PDF на одностраничные файлы с шаблонизатором имени.

Платформа: Windows. Язык: Python. Два портативных приложения — **каждое одним файлом `.exe`**, без установки Python и без соседних dll.

| Сборка | Назначение |
|---|---|
| `dist\PDFsplitter.exe` | Консоль: пакетная нарезка сразу по маске |
| `dist\PDFsplitterGUI.exe` | Окно: та же маска, превью страницы с масштабом и сдвигом, правка имени перед сохранением |

Исходники: `split_pdf.py` (движок + консоль), `split_gui.py` (GUI, использует движок). Сборка: `build.bat`. Очистка: `clean.bat`.

Движок нарезки — pikepdf. Превью в GUI — pypdfium2.

## 1. Маска имени

Маска = постоянный текст + переменные. Переменные две.

### 1.1 Нумерация `{НАЧАЛО:ШАГ}`

Числовой счётчик по страницам: значение = НАЧАЛО + индекс × ШАГ, индекс с нуля.

- шаг может быть отрицательным;
- если шаг опущен — `+1`;
- `{234:+1}` → 234, 235, 236…;
- `{10:-1}` → 10, 9, 8…;
- `{5}` → 5, 6, 7….

### 1.2 Дата `{DATE +ФОРМАТ}`

Текущие дата/время в формате strftime. Одна отметка на весь запуск (в GUI — на сессию, пока не нажмут «Сформировать имена» снова).

Пример: `{DATE +%d_%m_%y-%H%M%S}` → `30_08_26-143055`.

Если на листах разные даты (акты), дату пишут **обычным текстом** в маске (`27.06.2026`) и правят у конкретной страницы в GUI, глядя на превью. `{DATE +…}` — только когда одна и та же «сейчас» нужна сразу всем файлам.

### 1.3 Остальное

- неизвестные `{…}` остаются в имени как есть;
- после подстановки символы Windows `<>:"/\\|?*` заменяются на `_`;
- если нет расширения `.pdf` — оно добавляется;
- по умолчанию маска: `page_{1:+1}`.

Пример с токеном даты: `Акт о внедрении №{234:+1} {DATE +%d_%m_%y-%H%M%S}`.

Пример с датой текстом (типичный для GUI): `Акт о внедрении {216:+1} 27.06.2026` → `Акт о внедрении 216 27.06.2026.pdf`, `… 217 27.06.2026.pdf`, `… 218 27.06.2026.pdf`.

## 2. Консоль (`split_pdf.py` → PDFsplitter.exe)

**S1.** Один N-страничный PDF → N одностраничных PDF (pikepdf).

**S2.** Имя каждого файла считается из маски. Флаг: `-m` / `--mask`.

**S3.** Каталог вывода: `-d` / `--out-dir`. По умолчанию — рядом с исходным PDF.

Консоль не спрашивает имя и не показывает превью: сохраняет сразу.

```
PDFsplitter.exe входной.pdf [-d папка] [-m маска]
```

Исходники:

```
python split_pdf.py входной.pdf -d out -m "page_{1:+1}"
```

В cmd и PowerShell маску брать в кавычки, иначе `{…}` ломается.

Пустой PDF (0 страниц) — не ошибка, файлы не пишутся. Ошибки открытия/записи — код выхода 1.

## 3. GUI (`split_gui.py` → PDFsplitterGUI.exe)

Сначала маска формирует имена **всех** страниц, потом при просмотре листа можно поправить **текущее** имя.

Можно открыть файл аргументом: `PDFsplitterGUI.exe входной.pdf` или `python split_gui.py входной.pdf`.

### 3.1 Сценарий

1. Указать PDF, папку вывода и маску.
2. «Сформировать имена» (при выборе PDF через «Обзор» выполняется сразу): всем страницам проставляются номер по алгоритму и постоянный текст маски.
3. Слева — список имён, справа — превью текущей страницы.
4. Имя внизу уже из шаблона; его можно исправить по превью (например 3-я страница: `Акт о внедрении 218 15.06.2026` вместо даты из маски).
5. «Сохранить и далее» — записать этот лист и перейти к следующему. Enter в поле имени и Ctrl+S делают то же. «Сохранить все» — записать все текущие имена разом.

Повторное «Сформировать имена» пересчитывает весь список по маске; если уже были правки или сохранения — спрашивает подтверждение.

### 3.2 Список страниц

- пробел — имя как из шаблона;
- `*` — имя поправили вручную;
- `✓` — страница уже сохранена.

Листать: клик по списку, «Назад» / «Вперёд». Колёсико мыши на превью **не** меняет страницу — оно для прокрутки документа.

### 3.3 Превью: масштаб и сдвиг

Нужно рассмотреть дату/номер на листе перед правкой имени.

- «+» / «−» и подпись в процентах (100% — вся страница в окне);
- Ctrl+колёсико — масштаб к точке под курсором;
- «По странице» / Ctrl+0 — снова весь лист;
- полосы прокрутки по горизонтали и вертикали;
- колёсико — вверх/вниз;
- Shift+колёсико — влево/вправо;
- зажать левую кнопку и тянуть — сдвиг.

Масштаб сохраняется при переходе на другую страницу, сбрасывается при открытии нового PDF.

### 3.4 Сохранение

Пишется одностраничный PDF в выбранную папку с (возможно поправленным) именем. Недопустимые символы и `.pdf` обрабатываются так же, как в консоли.

«Сохранить все» при одинаковых именах у двух страниц — отказ с сообщением, ничего не пишется.

## 4. Состав репозитория

| Файл | Назначение |
|---|---|
| `split_pdf.py` | Движок маски и нарезки, консольный вход |
| `split_gui.py` | Окно |
| `requirements.txt` | pikepdf, pypdfium2, pyinstaller |
| `build.bat` | Сборка двух onefile-exe в `dist\` |
| `clean.bat` | Удаление `build\`, `dist\`, `.spec`, `__pycache__` (не трогает `.venv`) |
| `README.md` | Как пользоваться |
| `ТЗ.md` | Это задание |
| `.gitignore` | `.venv`, сборка, `*.pdf` |

В git не кладутся виртуальное окружение, собранные exe и рабочие PDF.

## 5. Сборка

Нужны Python (проверялось на 3.14) и `.venv` в корне проекта:

```
python -m venv .venv
build.bat
```

Результат — два самостоятельных файла: `dist\PDFsplitter.exe` и `dist\PDFsplitterGUI.exe`. Их можно копировать без Python.
